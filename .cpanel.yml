---
deployment:
  tasks:
    - /bin/bash -l -c "npm install -g pnpm --silent"
    - /bin/bash -l -c "pnpm install --frozen-lockfile"
    - /bin/bash -l -c "pnpm db:generate"
    - /bin/bash -l -c "pnpm db:push"
    - mkdir -p apps/web/tmp
    - touch apps/web/tmp/restart.txt
